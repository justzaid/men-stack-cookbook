<%- include('../partials/head') %>

  
<h1><%= title %></h1>

    <% users.forEach(user => { %>
        <div><a href="/users/<%= user._id %>"><%= user.username %></a></div>
    <% }); %>

<%- include('../partials/footer') %>